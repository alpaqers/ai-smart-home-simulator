#   copy .env.demo .env          (Windows)
#   cp .env.demo .env            (Linux/macOS)

AI_PROVIDER=gemini
GEMINI_API_KEY=replace-with-your-gemini-key
GEMINI_MODEL=gemini-3.5-flash

SERVER_TICK_INTERVAL_SECONDS=1
SERVER_AI_TICK_INTERVAL_SECONDS=86400